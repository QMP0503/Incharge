@model Incharge.ViewModels.ExpenseVM;

@{
    ViewData["Title"] = "Add Expense";
}

<div class="container">
    <div class="row">
        <h1 class="font-monospace fw-bold mt-3">Add Expense</h1>
    </div>
    <div class="form-group">
        <form asp-action="AddExpense" asp-route-Uuid="@Model.Uuid" method="post">

            <partial name="_ExpenseFormFields" model="Model" />

        </form>
    </div>
</div>

